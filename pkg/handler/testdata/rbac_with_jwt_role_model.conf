[request_definition]
r = sub, obj, act, roles

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = ((p.sub in r.roles) && regexMatch(r.obj, p.obj) && regexMatch(r.act, p.act))
